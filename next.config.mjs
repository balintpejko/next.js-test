/** @type {import('next').NextConfig} */
const nextConfig = {
   // output: "export",  

    reactStrictMode: true,
    optimization: {
        minimize: false, 
      },
};

export default nextConfig;
